@extends('layouts.app') @section('top_title','Hospital Appointments') @section('content')<div class="page-head"><div><h1 class="page-title">Appointments</h1><div class="page-subtitle">Review approved visits and record vaccination outcomes.</div></div></div><section class="table-card"><div class="table-wrap"><table class="table"><thead><tr><th>Child / Parent</th><th>Vaccine</th><th>Schedule</th><th>Status</th><th>Outcome</th></tr></thead><tbody>@forelse($appointments as $a)<tr><td><strong>{{$a->child->name}}</strong><div class="muted" style="font-size:11px">Parent: {{$a->child->parent->name}}</div></td><td>{{$a->vaccine->name}}<div class="muted" style="font-size:11px">{{$a->vaccine->dose}}</div></td><td>{{$a->appointment_date->format('d M Y')}}<div class="muted" style="font-size:11px">{{$a->appointment_time}}</div></td><td><span class="badge {{$a->status==='Approved'?'badge-success':($a->status==='Pending'?'badge-warning':'badge-neutral')}}">{{$a->status}}</span></td><td>@if($a->status==='Approved')<form method="POST" action="{{route('hospital.appointments.complete',$a)}}" style="display:flex;gap:6px;min-width:310px">@csrf @method('PUT')<select class="select" name="status"><option>Completed</option><option>Not Vaccinated</option></select><input class="input" name="hospital_note" placeholder="Note"><button class="btn btn-success">Save</button></form>@else<span class="muted">No action required</span>@endif</td></tr>@empty<tr><td colspan="5"><div class="empty">No appointments assigned.</div></td></tr>@endforelse</tbody></table></div></section>@endsection
